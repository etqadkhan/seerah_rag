# Hugging Face Spaces Configuration
# https://huggingface.co/docs/hub/spaces-config-reference

title: Seerah Q&A
emoji: 🕌
colorFrom: green
colorTo: blue
sdk: docker
app_port: 7860
pinned: false

# Hardware configuration (free tier)
hardware: cpu-basic

# Suggested secrets (set in HF Spaces UI):
# - GEMINI_API_KEY: Your Google Gemini API key
# - ALLOWED_USERNAMES: Comma-separated list of allowed usernames
# - MAX_USERS: Maximum number of users (default: 10)
