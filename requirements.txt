# Core Framework
streamlit>=1.28.0

# Google Gemini AI
google-generativeai>=0.3.0

# YouTube Data Extraction (Method 1 - Legacy, only needed if using transcript_fetcher)
yt-dlp>=2023.10.0  # Only needed for Method 1 (legacy YouTube fetching)
requests>=2.31.0  # Only needed for Method 1 (Supadata API)

# Vector Database
chromadb>=0.4.0

# Authentication
bcrypt>=4.0.0

# Environment Variables
python-dotenv>=1.0.0

# Text Processing
tiktoken>=0.5.0
nltk>=3.8.0

# Embeddings (HuggingFace - runs locally, free!)
sentence-transformers>=2.2.0

# HuggingFace Datasets (for loading cleaned transcripts)
datasets>=2.14.0
